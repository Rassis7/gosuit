// import axios from '../util/HttpAxios'
// const BASE_URL = ''

const state = {}

const getters = {}

const actions = {
  async enviarAvaliacao ({ commit }, params) {
    const res = new Promise((resolve, reject) => { return resolve(true) })
    return res
  }
}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
